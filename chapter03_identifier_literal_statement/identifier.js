//let class = "hello";
//console.log(class);//class is as reserved keyword in javascript, so it cannot be used as an identifier. This will result in syntax error

let undefined = null;
console.log(undefined);//it works but we should not do this

let returnValue = 5;//not allowed
